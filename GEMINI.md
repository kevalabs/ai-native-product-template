@AGENTS.md

Gemini CLI reads this file. The conventions live in `AGENTS.md` so
every coding agent shares one source — edit that, not this. Skills
are in `.agents/skills`, which Gemini CLI reads natively.
