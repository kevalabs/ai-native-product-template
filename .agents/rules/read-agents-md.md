---
trigger: always_on
---

# Read AGENTS.md first

Before doing anything in this repo, read `AGENTS.md` at the
repository root and follow it. It is the single source of agent
conventions for every coding agent; this rule exists only so
Antigravity loads it. Do not duplicate rules here — edit `AGENTS.md`.
